import { BaseAccordionConfig } from '../types';

export const CONFIG_DEFAULTS: BaseAccordionConfig = {
	firstActive: false,
	autoCollapse: false,
	immediate: false,
	speed: 0.4,
	ease: 'power2.inOut',
};
